interface:
  display_name: "Astro Design Loop Router"
  short_description: "Route separate clone and adaptation loops"
  default_prompt: "Use $astro-design-loop to route this work into a literal reference-clone loop first and a separate content-adaptation loop only after explicit clone approval."
