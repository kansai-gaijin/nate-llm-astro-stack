# Change request contract

Record the request ID, user wording, affected routes/components, acceptance checks, protected
behavior, and evidence paths in `workflow/update-state.json`. A request is complete only when its
audit receipt passes and all baseline verification remains green. Unrequested redesign, content
invention, dependency expansion, and reference-site recrawling are out of scope.
